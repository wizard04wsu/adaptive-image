{
	"name": "adaptive-image",
	"version": "1.0.0",
	"type": "module",
	"main": "dist/adaptive-image.js",
	"scripts": {
		"dev": "vite",
		"build": "vite build && node scripts/copy-build.js",
		"preview": "vite preview"
	},
	"devDependencies": {
		"vite": "^5.0.0"
	}
}
